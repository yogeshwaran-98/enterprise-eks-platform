apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "orders-service.fullname" . }}-test-connection"
  labels:
    {{- include "orders-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "orders-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
