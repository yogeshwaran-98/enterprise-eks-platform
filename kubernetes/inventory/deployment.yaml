apiVersion: apps/v1
kind: Deployment
metadata:
  name: inventory-service
  namespace: enterprise

spec:
  replicas: 2

  selector:
    matchLabels:
      app: inventory-service

  template:
    metadata:
      labels:
        app: inventory-service

    spec:
      containers:
        - name: inventory

          image: 106551436513.dkr.ecr.us-east-1.amazonaws.com/inventory-service:latest

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
