apiVersion: apps/v1
kind: Deployment
metadata:
  name: users-service
  namespace: enterprise

spec:
  replicas: 2

  selector:
    matchLabels:
      app: users-service

  template:
    metadata:
      labels:
        app: users-service

    spec:
      containers:
        - name: users

          image: 106551436513.dkr.ecr.us-east-1.amazonaws.com/users-service:latest

          ports:
            - containerPort: 8000

          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"

            limits:
              cpu: "500m"
              memory: "512Mi"
